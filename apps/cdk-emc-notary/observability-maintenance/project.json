{
  "name": "cdk-emcnotary-observability-maintenance",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/cdk-emc-notary/observability-maintenance/src",
  "projectType": "application",
  "tags": ["type:cdk", "domain:infra", "scope:observability-maintenance"],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/cdk-emc-notary/observability-maintenance",
        "format": ["cjs"],
        "bundle": true,
        "main": "apps/cdk-emc-notary/observability-maintenance/src/main.ts",
        "tsConfig": "apps/cdk-emc-notary/observability-maintenance/tsconfig.app.json",
        "assets": [
          "apps/cdk-emc-notary/observability-maintenance/cdk.json"
        ],
        "generatePackageJson": true,
        "esbuildOptions": {
          "sourcemap": true,
          "external": ["aws-sdk"]
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/apps/cdk-emc-notary/observability-maintenance"
      ],
      "dependsOn": ["build", "^build"],
      "options": {
        "jestConfig": "apps/cdk-emc-notary/observability-maintenance/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "synth": {
      "executor": "nx:run-commands",
      "options": {
        "command": "FEATURE_CDK_EMCNOTARY_STACKS_ENABLED=1 node ../../../../dist/libs/infra/config-loader/bin/cdk-synth.js",
        "cwd": "dist/apps/cdk-emc-notary/observability-maintenance"
      },
      "dependsOn": [
        "build",
        "infra-config-loader:build",
        "cdk-emcnotary-core:admin:test:core-deployed",
        "cdk-emcnotary-instance:admin:test:instance-deployed"
      ]
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "FEATURE_CDK_EMCNOTARY_STACKS_ENABLED=1 node ../../../../dist/libs/infra/config-loader/bin/cdk-deploy.js deploy --require-approval never ${DOMAIN:+--context domain=$DOMAIN} ${CORE_PARAM_PREFIX:+--context coreParamPrefix=$CORE_PARAM_PREFIX} ${INSTANCE_PARAM_PREFIX:+--context instanceParamPrefix=$INSTANCE_PARAM_PREFIX}",
        "cwd": "dist/apps/cdk-emc-notary/observability-maintenance"
      },
      "dependsOn": [
        "build",
        "infra-config-loader:build",
        "cdk-emcnotary-core:admin:test:core-deployed",
        "cdk-emcnotary-instance:admin:test:instance-deployed"
      ]
    },
    "diff": {
      "executor": "nx:run-commands",
      "options": {
        "command": "FEATURE_CDK_EMCNOTARY_STACKS_ENABLED=1 cdk diff ${DOMAIN:+--context domain=$DOMAIN} ${CORE_PARAM_PREFIX:+--context coreParamPrefix=$CORE_PARAM_PREFIX} ${INSTANCE_PARAM_PREFIX:+--context instanceParamPrefix=$INSTANCE_PARAM_PREFIX}",
        "cwd": "dist/apps/cdk-emc-notary/observability-maintenance"
      },
      "dependsOn": [
        "build",
        "infra-config-loader:build",
        "cdk-emcnotary-core:admin:test:core-deployed",
        "cdk-emcnotary-instance:admin:test:instance-deployed"
      ]
    },
    "destroy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "FEATURE_CDK_EMCNOTARY_STACKS_ENABLED=1 node ../../../../dist/libs/infra/config-loader/bin/cdk-deploy.js destroy --force ${DOMAIN:+--context domain=$DOMAIN} ${CORE_PARAM_PREFIX:+--context coreParamPrefix=$CORE_PARAM_PREFIX} ${INSTANCE_PARAM_PREFIX:+--context instanceParamPrefix=$INSTANCE_PARAM_PREFIX}",
        "cwd": "dist/apps/cdk-emc-notary/observability-maintenance"
      },
      "dependsOn": ["build", "infra-config-loader:build"]
    },
    "admin:info": {
      "executor": "nx:run-commands",
      "dependsOn": ["admin-stack-info:build"],
      "options": {
        "command": "AWS_PROFILE=${AWS_PROFILE:-hepe-admin-mfa} AWS_REGION=${AWS_REGION:-us-east-1} APP_PATH=apps/cdk-emc-notary/observability-maintenance DOMAIN=${DOMAIN:-emcnotary.com} pnpm nx run admin-stack-info:get"
      },
      "description": "Get observability-maintenance stack information"
    },
    "admin:events": {
      "executor": "nx:run-commands",
      "dependsOn": ["admin-stack-events:build"],
      "options": {
        "command": "AWS_PROFILE=${AWS_PROFILE:-hepe-admin-mfa} AWS_REGION=${AWS_REGION:-us-east-1} APP_PATH=apps/cdk-emc-notary/observability-maintenance DOMAIN=${DOMAIN:-emcnotary.com} MAX_RESULTS=${MAX_RESULTS:-50} pnpm nx run admin-stack-events:events"
      },
      "description": "Get recent CloudFormation stack events"
    },
    "admin:events:failed": {
      "executor": "nx:run-commands",
      "dependsOn": ["admin-stack-events:build"],
      "options": {
        "command": "AWS_PROFILE=${AWS_PROFILE:-hepe-admin-mfa} AWS_REGION=${AWS_REGION:-us-east-1} APP_PATH=apps/cdk-emc-notary/observability-maintenance DOMAIN=${DOMAIN:-emcnotary.com} FAILED_ONLY=1 pnpm nx run admin-stack-events:events"
      },
      "description": "Get only failed CloudFormation stack events"
    },
    "admin:availability:report": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "^build",
        "admin-stack-info:build",
        "cdk-emcnotary-instance:admin:test:bootstrap-complete"
      ],
      "options": {
        "command": "AWS_PROFILE=${AWS_PROFILE:-hepe-admin-mfa} AWS_REGION=${AWS_REGION:-us-east-1} APP_PATH=apps/cdk-emc-notary/instance DOMAIN=${DOMAIN:-emcnotary.com} OUTPUT_FILE=${OUTPUT_FILE} pnpm exec tsx --tsconfig tools/tsconfig.json tools/availability-report.cli.ts ${DOMAIN:+--domain $DOMAIN} ${OUTPUT_FILE:+--output $OUTPUT_FILE}"
      },
      "description": "Generate availability report using instance runtime checks"
    },
    "admin:cleanup:disk-space": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "^build",
        "admin-ssh:build",
        "admin-stack-info:build",
        "cdk-emcnotary-instance:admin:ssh:test"
      ],
      "options": {
        "command": "AWS_PROFILE=${AWS_PROFILE:-hepe-admin-mfa} AWS_REGION=${AWS_REGION:-us-east-1} APP_PATH=apps/cdk-emc-notary/instance DOMAIN=${DOMAIN:-emcnotary.com} pnpm exec tsx --tsconfig tools/tsconfig.json tools/cleanup-disk-space.cli.ts ${DOMAIN:+--domain $DOMAIN} ${DRY_RUN:+--dry-run}"
      },
      "description": "Clean disk space on instance via observability wrapper"
    },
    "admin:backup-and-cleanup": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "admin-ssh:build", "admin-stack-info:build"],
      "options": {
        "command": "AWS_PROFILE=${AWS_PROFILE:-hepe-admin-mfa} AWS_REGION=${AWS_REGION:-us-east-1} APP_PATH=apps/cdk-emc-notary/instance DOMAIN=${DOMAIN:-emcnotary.com} STACK_NAME=${STACK_NAME} DESTINATION_DIR=${DESTINATION_DIR} JOURNAL_VACUUM_MB=${JOURNAL_VACUUM_MB:-100} SSH_KEY_PATH=${SSH_KEY_PATH} pnpm exec tsx --tsconfig tools/tsconfig.json tools/backup-and-cleanup-mailserver.cli.ts ${DOMAIN:+--domain $DOMAIN} ${STACK_NAME:+--stack-name $STACK_NAME} ${DESTINATION_DIR:+--destination-dir $DESTINATION_DIR} ${JOURNAL_VACUUM_MB:+--journal-vacuum-mb $JOURNAL_VACUUM_MB} ${SSH_KEY_PATH:+--ssh-key-path $SSH_KEY_PATH} ${SKIP_BACKUP:+--skip-backup} ${SKIP_CLEANUP:+--skip-cleanup} ${NO_TAR:+--no-tar} ${DRY_RUN:+--dry-run}"
      },
      "description": "Backup and cleanup workflow wrapper for runtime maintenance"
    },
    "admin:disk:monitor": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "^build",
        "admin-ssh:build",
        "admin-stack-info:build",
        "cdk-emcnotary-instance:admin:ssh:test"
      ],
      "options": {
        "command": "AWS_PROFILE=${AWS_PROFILE:-hepe-admin-mfa} AWS_REGION=${AWS_REGION:-us-east-1} APP_PATH=apps/cdk-emc-notary/instance DOMAIN=${DOMAIN:-emcnotary.com} pnpm exec tsx --tsconfig tools/tsconfig.json tools/monitor-disk-space.cli.ts ${DOMAIN:+--domain $DOMAIN} ${THRESHOLD:+--threshold $THRESHOLD}"
      },
      "description": "Monitor disk space on instance via observability wrapper"
    },
    "admin:disk:expand-root": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "^build",
        "admin-stack-info:build",
        "cdk-emcnotary-instance:admin:test:instance-deployed"
      ],
      "options": {
        "command": "AWS_PROFILE=${AWS_PROFILE:-hepe-admin-mfa} AWS_REGION=${AWS_REGION:-us-east-1} APP_PATH=${APP_PATH:-apps/cdk-emc-notary/instance} DOMAIN=${DOMAIN:-emcnotary.com} TARGET_SIZE_GB=${TARGET_SIZE_GB:-20} pnpm exec tsx --tsconfig tools/tsconfig.json tools/expand-root-volume.cli.ts --target-size-gb ${TARGET_SIZE_GB:-20} ${DOMAIN:+--domain $DOMAIN} ${APP_PATH:+--app-path $APP_PATH}"
      },
      "description": "Expand root EBS volume in place with dry-run default (pass --execute to apply)"
    }
  }
}
