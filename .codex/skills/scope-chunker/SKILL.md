---
name: scope-chunker
description: >
  Decomposes a completed intent packet into typed work orders with bounded file scope and verification plans.
allowed-tools:
  - Read
  - Write
  - Bash
  - Grep
  - Glob
---

# Scope chunker (Codex overlay)

**Authoritative procedure:** read and follow `.foundry/skills/scope-chunker/SKILL.md` at the repository root. Invoke with `$scope-chunker` after `$define-intent` when DoR is met.
