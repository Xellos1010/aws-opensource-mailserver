# shared-constructs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-constructs` to build the library.

## Running unit tests

Run `nx test shared-constructs` to execute the unit tests via [Jest](https://jestjs.io).
