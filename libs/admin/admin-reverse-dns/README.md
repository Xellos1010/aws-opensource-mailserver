# admin-reverse-dns

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build admin-reverse-dns` to build the library.

## Running unit tests

Run `nx test admin-reverse-dns` to execute the unit tests via [Vitest](https://vitest.dev/).
