{
  "name": "admin-reverse-dns",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/admin/admin-reverse-dns/src",
  "projectType": "library",
  "tags": ["scope:aws", "type:util", "domain:dns"],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/admin/admin-reverse-dns",
        "main": "libs/admin/admin-reverse-dns/bin/set-reverse-dns.ts",
        "outputFileName": "set-reverse-dns.mjs",
        "bundle": true,
        "platform": "node",
        "format": ["esm"],
        "target": "node20",
        "tsConfig": "libs/admin/admin-reverse-dns/tsconfig.lib.json",
        "esbuildOptions": {
          "outExtension": {
            ".js": ".mjs"
          }
        }
      }
    },
    "set": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node dist/libs/admin/admin-reverse-dns/set-reverse-dns.mjs"
      }
    },
    "set:sampletenant": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "AWS_PROFILE=your-aws-profile AWS_REGION=us-east-1 APP_PATH=apps/clients/cdk-client-example node dist/libs/admin/admin-reverse-dns/set-reverse-dns.mjs"
      }
    },
    "set:mailexample": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "AWS_PROFILE=your-aws-profile AWS_REGION=us-east-1 APP_PATH=apps/clients/cdk-client-example/core node dist/libs/admin/admin-reverse-dns/set-reverse-dns.mjs"
      }
    },
    "set:sample-net": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "AWS_PROFILE=your-aws-profile AWS_REGION=us-east-1 APP_PATH=apps/clients/cdk-client-example/instance node dist/libs/admin/admin-reverse-dns/set-reverse-dns.mjs"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/admin/admin-reverse-dns/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
