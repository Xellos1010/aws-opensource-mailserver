# admin-credentials

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build admin-credentials` to build the library.

## Running unit tests

Run `nx test admin-credentials` to execute the unit tests via [Vitest](https://vitest.dev/).
