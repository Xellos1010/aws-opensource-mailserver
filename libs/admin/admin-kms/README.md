# admin-kms

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build admin-kms` to build the library.

## Running unit tests

Run `nx test admin-kms` to execute the unit tests via [Jest](https://jestjs.io).
