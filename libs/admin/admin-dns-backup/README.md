# admin-dns-backup

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build admin-dns-backup` to build the library.

## Running unit tests

Run `nx test admin-dns-backup` to execute the unit tests via [Jest](https://jestjs.io).
