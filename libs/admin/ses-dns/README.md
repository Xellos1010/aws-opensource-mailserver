# ses-dns

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ses-dns` to build the library.

## Running unit tests

Run `nx test ses-dns` to execute the unit tests via [Vitest](https://vitest.dev/).
