# admin-mail-backup

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build admin-mail-backup` to build the library.

## Running unit tests

Run `nx test admin-mail-backup` to execute the unit tests via [Jest](https://jestjs.io).
