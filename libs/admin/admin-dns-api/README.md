# admin-dns-api

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build admin-dns-api` to build the library.

## Running unit tests

Run `nx test admin-dns-api` to execute the unit tests via [Vitest](https://vitest.dev/).
