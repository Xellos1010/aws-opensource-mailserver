import { adminBackupBridge } from "./admin-backup-bridge";

describe("adminBackupBridge", () => {
  it("should work", () => {
    expect(adminBackupBridge()).toEqual("admin-backup-bridge");
  });
});
